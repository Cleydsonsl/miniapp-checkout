import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  headerButton: {
    paddingLeft: 20,
    paddingRight: 20,
  },
  badge: {
    position: 'absolute',
    top: 8,
    left: 10,
  }
});

export default styles